{
  "name": "antiswap",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "npx tailwindcss -i ./index.css -o ./dist/output.css",
    "dev": "npm run build -- --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  }
}
